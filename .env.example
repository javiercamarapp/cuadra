# ─── WhatsApp Cloud API (Meta) — número NUEVO de Cuadra ───────────────────
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_VERIFY_TOKEN=            # token que tú inventas para verificar el webhook
WHATSAPP_APP_SECRET=

# ─── OpenRouter (gateway LLM único: Gemini + Claude, con ZDR forzado) ─────
OPENROUTER_API_KEY=
# Overrides opcionales por rol (si se omiten, usan los defaults de
# src/lib/llm/models.ts). Slugs de OpenRouter:
# CUADRA_MODEL_OCR=google/gemini-3.6-flash
# CUADRA_MODEL_CUADRE=anthropic/claude-sonnet-5
# CUADRA_MODEL_CUADRE_FALLBACK=anthropic/claude-opus-5
# CUADRA_MODEL_CHAT=google/gemini-3.5-flash-lite
# CUADRA_MODEL_ROUTER=google/gemini-3.5-flash-lite
# Plan B demo en vivo (llaves directas por si OpenRouter cae):
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=

# ─── Supabase (DB + auth) ─────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ─── Facturapi (validación CFDI / Carta Porte) ────────────────────────────
FACTURAPI_KEY=

# ─── Upstash (colas / rate-limit) ─────────────────────────────────────────
UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=
QSTASH_TOKEN=

# ─── Sentry (observabilidad) ──────────────────────────────────────────────
SENTRY_DSN=

# ─── App ──────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000   # en Vercel: el dominio de producción

# ─── Flags de comportamiento (encendidos para el demo — ver AUDIT_V3) ─────
# Con TODOS apagados, el camino es el verificado "clásico". El demo corre CON
# estos ON. Deben replicarse en el entorno de Vercel.
CUADRA_INTAKE_GRACE_MS=2000          # gracia anti-carrera fotos+"listo"
CUADRA_RECUPERAR_CIERRE_PARCIAL=1    # recupera cierre si el ciclo muere tras guardar
CUADRA_DEDUP_FOTOS=1                 # dedup de fotos por SHA-256
CUADRA_INTAKE_ESPERA_MS=20000        # tope de la barrera de intake (acota peor caso)
# Opcional: subir SOLO si el plan de Vercel es Pro + Fluid Compute (Hobby ignora >60):
# el maxDuration vive en src/app/api/webhook/whatsapp/route.ts

# Secreto de servidor para el HMAC de la cookie del panel. OBLIGATORIO en
# producción: sin él, el HMAC se deriva del propio passcode y una cookie
# capturada permite crackearlo offline. Cualquier cadena larga y aleatoria.
DASHBOARD_SECRET=

# Observabilidad (opcional). Sin DSN no se carga el paquete siquiera.
# Al ponerlo, Sentry pasa a ser SUBENCARGADO: ver docs/conocimiento/52-anexo-subencargados.md
SENTRY_DSN=
